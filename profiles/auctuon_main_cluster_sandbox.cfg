[buildout]
extends =
    profiles/preview.cfg

parts +=
    penstock.yaml

[scripts]
eggs += penstock

[project]
with_penstock = 1

[sentinel.conf]
<= config-from-template
quorum = 2

[couchdb.ini]
replicator_checkpoint_interval = 1000


